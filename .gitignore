node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files - NEVER commit these
.env
.env.local
.env.production
.env.*.local

# PDF storage - local only, not synced
stored_pdfs/